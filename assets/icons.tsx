import React from "react";
import { AntDesign, Feather } from "@expo/vector-icons";

const icons = {
    index: (props: any) => <AntDesign name="home" size={26} {...props} />,
    //settings: (props: any) => <AntDesign name="user" size={26} {...props} />,
};

export default icons;